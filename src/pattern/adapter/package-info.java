package pattern.adapter;

/*
   Adapter pattern works as bridge between two incompatable interfaces

   This pattern involve a single class which is respsible to join functionality of independent or incompatable interfaces

   When  some object needs to use other class with incompatable interface

   When uoy want to create a reusable class that cooperates with classes which hava compataible interfaces

   Addapter patter as wrapper of some interface
 */